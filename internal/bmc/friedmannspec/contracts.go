package friedmannspec

// Allowed status values for maps, branches, and checks
const (
	StatusCandidateOnly = "candidate_only"
	StatusBlocked       = "blocked"
	StatusContested     = "contested"
)

// Allowed spec scope values
const (
	SpecScopeCandidateOnly = "candidate_specification_only"
	SpecScopeBlocked       = "blocked"
	SpecScopeContested     = "contested"
)
